const usuario = {
    nome: "João",
    idade: 25,
    profissao: "programador",
    cor_preferida: "verde",
}

const { nome, idade, profissao, cor_preferida } = usuario;

console.log(`Olá... eu sou ${nome}, tenho ${idade} anos de idade. Sou ${profissao} e minha cor preferida é ${cor_preferida}`)